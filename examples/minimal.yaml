 notify:
  - name: minimal
    platform: supernotify
    # notify entity, smtp and mobile push automatically configured
    # recipients and mobile devices automatically pulled in from Home Assistant
